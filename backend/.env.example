# Database Configuration
# Replace the values below with your PostgreSQL connection details
DB_NAME=grafidb
DB_USER=postgres
DB_PASSWORD=yourpassword
DB_HOST=localhost
DB_PORT=5432

# Alternatively, you can use a single DATABASE_URL
# DATABASE_URL=postgres://postgres:yourpassword@localhost:5432/grafidb
